#!/bin/zsh

xbacklight -inc $1

#xbacklight doesn't work when optimus is disabled

#BL_NAME=radeon_bl0
#INCR_AMOUNT=$1

#CURRENT=$(cat /sys/class/backlight/$BL_NAME/brightness)
#NEW=$((CURRENT+INCR_AMOUNT))

#sudo bash -c "echo $NEW > /sys/class/backlight/$BL_NAME/brightness"
